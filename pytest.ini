[pytest]
addopts = -q -m "not integration"
testpaths = tests
markers =
  integration: tests that hit the real database (and may use network)
  live_eval: evaluation tests that may call external LLMs/DB
  agent_eval: agent evaluation — JSONL harness expectations plus failure/adversarial behavior (offline)
